# AC Tech Labs Runbook

Date: 2026-05-13

Status: non-public staged candidate. Do not submit the form, book a call, or publish without fresh action-time confirmation.

## Why This Exists

Afterbuild and Bytewise are already in market. If both stay silent or come back negative, the next candidate should not require reopening the whole target list.

This runbook narrows the next decision to one exact target and one exact public route:

```text
If we choose AC Tech Labs next, use the public contact form only with an explicitly truthful validation framing.
```

## Current Readout

- Afterbuild email sent: 2026-05-11 23:07:39 CST.
- Bytewise email sent: 2026-05-12 19:21:35 CST.
- Afterbuild reply status: no reply observed yet.
- Bytewise reply status: no reply observed yet.
- GitHub issue #1: still open, no comments.
- X notifications: still no useful new response after the Jesse stability-first URL follow-up.
- Live site: HTTP 200.

## Why AC Tech Labs Next

AC Tech Labs is the sharpest remaining third candidate because:

- its public positioning explicitly targets Bolt/Lovable MVP rescue and production hardening
- it has a readiness-score framing that overlaps SpecRelay's pre-handoff assessment surface
- the core validation question is different enough from Afterbuild and Bytewise to teach something new:

```text
Is a founder-filled narrative packet useful before a readiness score / fit-call workflow, or is the score plus repo access already enough?
```

## Public Route

Preferred route: `https://aclabs.io/contact`

Observed public framing:

- page headline: `Tell us about your MVP`
- support text: `6 questions to see if we're a fit. We reply within 1 business day.`
- CTA: `Submit & book a call`
- alternate CTA across the site: `Book a 20-min fit call`

There is no public email route visible in the current public surface captured for this run.

## Route Assessment

This route is usable, but weaker than Afterbuild and Bytewise for one reason: it is built for an actual rescue lead, not a lightweight workflow-validation ask.

Required fields currently visible:

- `Your name`
- `Email`
- `Prototype source` with `Bolt`, `Lovable`, `Other`
- `Tech stack`
- `Users / revenue today`
- `Must-have date`
- `Link to repo or demo`

Implication:

- `Users / revenue today` and `Must-have date` push the interaction toward a real project intake.
- That means the form can distort the experiment if we answer like a fake rescue client.
- The only acceptable path is to answer truthfully and explicitly state that this is a validation question, not a paid rescue lead.

## Exact Truthful Field Strategy

If this route is approved, use the form with truthful values only:

- `Your name`: the current sender identity for the outreach turn
- `Email`: `ws.software@outlook.com`
- `Prototype source`: `Other`
- `Tech stack`:
  `Static web MVP. No backend. Zero-login tool that generates stability-first handoff packets for AI-built apps.`
- `Users / revenue today`:
  `0 users, 0 revenue. This is a validation prototype, not an active client rescue request.`
- `Must-have date`:
  use a real near-term date only if the form requires one to submit; if used, explain in the freeform ask that there is no rescue deadline and this is only a validation check
- `Link to repo or demo`:
  `https://willsuo-github.github.io/specrelay/`

## Staged Form Message

If the form exposes a freeform message field or notes box, use:

```text
Hi AC Tech Labs team,

I noticed your Bolt/Lovable readiness score and rescue/hardening positioning.

I am testing a neighboring idea: before a founder books a fit call or asks for a hardening sprint, should they produce a stability-first handoff packet?

SpecRelay captures core flows, observed issues, affected QA paths, risk surface, integrations, and a short engineering brief. It is more narrative than a readiness score and less technical than a repo audit.

The useful feedback is whether this helps before the first fit-call/readiness pass, or whether a checklist/score plus repo access is already enough.

Live draft: https://willsuo-github.github.io/specrelay/

No sales ask. This is a validation check on whether the packet should exist as an intake aid before agencies touch an AI-built MVP.
```

## Validation Checklist

Before submitting, verify all of these:

1. Afterbuild and Bytewise still have not produced a strong positive reply.
2. The route still requires the same fit-call form and no cleaner public email route has appeared.
3. The form can be filled truthfully without pretending to be an active rescue client.
4. The freeform note clearly states that this is a validation check, not a disguised sales inquiry.
5. The sender email in the form is `ws.software@outlook.com`.

## Readout Interpretation

Strong signal:

- they say the packet would help before or alongside the readiness-score / fit-call step
- they say founders should bring this before the first real review
- they point to one specific packet section that improves their intake

Weak signal:

- generic encouragement
- agreement that readiness matters without saying the packet would be used

Negative signal:

- the score/form plus repo access already solves the job
- narrative packet is redundant before their first technical review

## Exact Remaining Approval Gate

The narrowest remaining action in this runbook is:

```text
If Afterbuild and Bytewise still have not produced a useful reply, confirm submitting the AC Tech Labs contact form with the truthful field mapping and validation message above.
```
